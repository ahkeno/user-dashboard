import { environment } from './../../environments/environment';
export const API = { 
    urlUser: `${environment.MOCK}` +'user',
    urlUserCareer : `${environment.MOCK}` +'career',
    urlDocument:`${environment.MOCK}` +'document'
}
